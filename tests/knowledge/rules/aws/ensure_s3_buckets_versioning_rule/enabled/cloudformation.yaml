AWSTemplateFormatVersion: "2010-09-09"

Resources:
  S3BucketVersioningEnabled:
    Type: 'AWS::S3::Bucket'
    Properties:
      BucketName: my-versioning-bucket-dev-idan
      VersioningConfiguration:
        Status: Enabled
