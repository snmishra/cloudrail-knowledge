rules_metadata:
  # TODO: Fix this rule's metadata
  - rule_id: non_car_gcp_sql_database_ssl_required
    cloud_provider: google_cloud_provider
    name: Ensure SQL database requires SSL
    severity: major
    description: Ensure SQL database requires SSL
    human_readable_logic: Ensure SQL database requires SSL
    remediation_steps:
      terraform: Ensure SQL database requires SSL
      console: Ensure SQL database requires SSL
    rule_type: non_context_aware
    security_layer: networking
    resource_type:
      - database

  # TODO: Fix this rule's metadata
  - rule_id: CKV_GCP_6
    cloud_provider: google_cloud_provider
    name: Ensure all Cloud SQL database instance requires all incoming connections to use SSL
    severity: medium
    description: Ensure all Cloud SQL database instance requires all incoming connections to use SSL
    human_readable_logic: Ensure all Cloud SQL database instance requires all incoming connections to use SSL
    remediation_steps:
      terraform: Ensure all Cloud SQL database instance requires all incoming connections to use SSL
      console: Ensure all Cloud SQL database instance requires all incoming connections to use SSL
    rule_type: non_context_aware
    security_layer: storage
    resource_type:
      - storage

  # TODO: Fix this rule's metadata
  - rule_id: CKV_GCP_4
    cloud_provider: google_cloud_provider
    name: Ensure no HTTPS or SSL proxy load balancers permit SSL policies with weak cipher suites
    severity: medium
    description: Ensure no HTTPS or SSL proxy load balancers permit SSL policies with weak cipher suites
    human_readable_logic: Ensure no HTTPS or SSL proxy load balancers permit SSL policies with weak cipher suites
    remediation_steps:
      terraform: Ensure no HTTPS or SSL proxy load balancers permit SSL policies with weak cipher suites
      console: Ensure no HTTPS or SSL proxy load balancers permit SSL policies with weak cipher suites
    rule_type: non_context_aware
    security_layer: storage
    resource_type:
      - storage
